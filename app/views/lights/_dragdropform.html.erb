<%= form_for([@location, @light]) do |f| %>
  <% if @light.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@light.errors.count, "error") %> prohibited this light from being saved:</h2>

      <ul>
      <% @light.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">

    <%= f.hidden_field :location_id %>
  </div>
  <div class="field">

    <%= f.hidden_field :x_position %>
  </div>
  <div class="field">

    <%= f.hidden_field :y_position %>
  </div>
  <div class="field">

    <%= f.hidden_field :id_code %>
  </div>
  <div class="field">

    <%= f.hidden_field :content %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
